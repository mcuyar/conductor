require 'spec_helper'

describe Vagrant::Conductor do
  it 'has a version number' do
    expect(Vagrant::Conductor::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
